// let age;

// console.log(age, age + 3, `the age is ${age}`);

// undefined, NaN, and "the age is undefined"

let age = null;

console.log(age, age + 3, `the age is ${age}`);

// null, 3, and "the age is null"